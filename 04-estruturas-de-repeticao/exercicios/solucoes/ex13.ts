let consultasRestantes = 0;

do {
    console.log(`Consultando atualizações...`);
    consultasRestantes--
} while (consultasRestantes > 0);

const explicacao: string = `A mensagem aparece mesmo com o valor inicial igual a zero, porque esse é o propósito da existência do do...while: a ideia é que uma primeira execução seja garantida mesmo que a condição não seja satisfeita.`;
