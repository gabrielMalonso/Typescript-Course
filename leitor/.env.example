# Public identifiers only.
VITE_CONVEX_URL=
# Convex CLI selection (local only)
CONVEX_DEPLOYMENT=
# Worker configuration in .dev.vars (never commit secrets):
# WORKOS_API_KEY, WORKOS_CLIENT_ID, OWNER_WORKOS_USER_ID, STUDY_COOKIE_PASSWORD (32+ random characters)
