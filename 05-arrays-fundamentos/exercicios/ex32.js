var leituras = [18, 21, 19];
if (leituras.length > 0) {
    console.log("Primeiro elemento: ".concat(leituras[0]));
    console.log("\u00DAltimo elemento: ".concat(leituras[leituras.length - 1]));
}
else {
    console.log("Nenhuma leitura dispon\u00EDvel.");
}
